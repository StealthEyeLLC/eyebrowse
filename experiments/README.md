# experiments

Experiments answer architectural questions without becoming canonical automatically.

Build 001 begins with:

```text
apc/
```

for live Chrome 151 Annotated Page Content decoding/coverage/latency experiments.

Later experiments may cover lifecycle identity, WebMCP, third-party DevTools tools, canvas/vision, browser UI, or Chromium modifications.

A successful experiment becomes architecture only when its result is promoted into the numbered canonical documents in `../docs/`.
